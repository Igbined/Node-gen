{
  "version": 2,
  "builds": [
    { "src": "api/**/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/U/(?<projectName>[^/]+)", "dest": "/api/U?projectName=$projectName" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}